<!DOCTYPE html>

<html lang="pt-br">

<head>
    <link rel="stylesheet" type="text/css" href="css/styles.css" />
    <meta charset="UTF-8">
    <title>Receitas deliciosas!</title>
</head>

<body>
    <h1 id="header">Receitas incríveis para você!</h1>
    <img src="images/chef.svg" alt="um chapéu de cozinheiro" style="width:800px;height:450px;">
    <h3 id="imagelabel">Uma coletânea de receitas do chefe</h3>
    <div>
        <ul>
            <% if (recipes && recipes.length > 0) { %>
                <% recipes.forEach(function(recipe) { %>
                    <li>
                        <a href="/receitas/<%= recipe.nome.toLowerCase().replace(/\s+/g, '-') %>"><%= recipe.nome %></a>
                    </li> 
                <% }); %>
            <% } else { %>
                <li>Nenhuma receita disponível no momento.</li>
            <% } %>
        </ul>
    </div>
    <a href="#header">Retornar ao topo</a>
</body>

</html>
